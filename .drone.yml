kind: pipeline
type: docker
image: androidsdk/android-30
script:
  - ./gradlew test assemble -PdisablePreDex
trigger:
  event:
    - cron
    - custom
    - push
    - pull_request
    - tag
    - promote
    - rollback
