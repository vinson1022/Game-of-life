kind: pipeline
type: exec
name: default

platform:
  os: darwin

steps:
- name: test
  commands:
  - ./gradlew test

trigger:
  event:
    - cron
    - custom
    - push
    - pull_request
    - tag
    - promote
    - rollback
