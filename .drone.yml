kind: pipeline
type: exec
name: default

steps:
- name: test
  commands:
  - chmod +x ./gradlew && ./gradlew test

trigger:
  event:
    - cron
    - custom
    - push
    - pull_request
    - tag
    - promote
    - rollback
